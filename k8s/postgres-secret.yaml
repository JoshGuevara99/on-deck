# STUB — never commit real values here.
# Populate via Sealed Secrets, External Secrets Operator, or your CI/CD pipeline.
# Swap the database-url value when migrating from local Postgres to a cluster database.
apiVersion: v1
kind: Secret
metadata:
  name: on-deck-secrets
type: Opaque
stringData:
  database-url: "postgresql://<db-user>:<db-pass>@postgres-service:5432/ondeck"
  clerk-secret-key: "<clerk-secret-key>"
