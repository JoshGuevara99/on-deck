# STUB — do not commit real values.
# Use Sealed Secrets, External Secrets Operator, or your CI/CD pipeline
# to inject real credentials. Swap DATABASE_URL here when migrating
# from local Postgres to a managed cluster database.
apiVersion: v1
kind: Secret
metadata:
  name: on-deck-secrets
type: Opaque
stringData:
  database-url: "postgresql://user:password@postgres-service:5432/ondeck"
  clerk-secret-key: ""
